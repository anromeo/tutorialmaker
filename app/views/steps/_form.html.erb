<%= form_for([@tutorial, @step]) do |f| %>
	<% if @step.errors.present? %>
	<%	@step.errors.full_message.each do |attribute, msg| %>
			<p><%= msg %></p>
		<% end %>
	<% end %>
	<table>
		<tr>
			<td>Description</td>
		</tr>
		<tr>
			<td><%= f.text_area :description, {:id=>'description'} %></td>
		</tr>
		<tr>
			<td><%= if @enter
								f.submit 'Edit'
							else
								f.submit 'Create'
							end %></td>
		</tr>
	</table>
<% end %>